services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '${DB_PORT:-5432}:5432'
    environment:
      POSTGRES_USER: ${DB_USERNAME:-prospect_pulse}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-prospect_pulse}
      POSTGRES_DB: ${DB_DATABASE:-prospect_pulse}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
